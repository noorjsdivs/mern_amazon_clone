import Header from "./Header";

const MainHeader = () => {
      return (
            <div className="sticky z-50 top-0">
                  <Header />
            </div>
      );
};

export default MainHeader;